- Changelog added to F-Droid
- Web extraction: now extracts BPM if provided
- Bug fixes:
     > Save setlist only when changed,
     > catch null pointers while wakelock acquiring and restore app from background,
     > Improve Web extraction - fixed lost whitespaces after multiple new lines